spring:
  application:
    name: spring-boot-rabbitmq
  rabbitmq:
    # 连接地址
    host: 127.0.0.1
    # 端口号，此为默认端口
    port: 5672
    # 连接用户名，默认
    username: guest
    # 连接密码，默认
    password: guest
logging:
  config: classpath:log4j2.xml